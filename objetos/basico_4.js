const usuario = {
    email: 'morais.sda@gmail.com'
};

usuario.nome = 'iago';//pode ser declarado fora do {} novas chaves e metodos.
usuario.senha = '0000';

console.log(usuario.email);
console.log(usuario.nome);
console.log(usuario.senha);
console.log(` nome: ${usuario.nome} 
email: ${usuario.email} 
senha: ${usuario.senha}`);
console.log(usuario);